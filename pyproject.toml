[project]
name = "fastapi-framework"
version = "0.1.0"
description = "Reusable FastAPI framework template with auth, logging, and standardized responses"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.123.0",
    "loguru>=0.7.3",
    "pydantic-settings>=2.12.0",
    "python-dotenv>=1.2.1",
    "uvicorn>=0.38.0",
]
