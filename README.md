# **How to run?**
1. create a virtual environment <p>
    `uv venv`
2. activate environment <p>
    `source .venv/bin/activate`
3. install all the required packages <p>
    `uv sync`
4. run the program <p>
    `uv run app.main:app --reload`